# OWASAKA Frontends

This directory holds the React frontend workstream for OWASAKA.

- `frontend/react`: Vite + React + TypeScript operator console.
- `../web`: existing SvelteKit operational dashboard.

The React and Svelte frontends share product language, colors, and evidence workflows, but remain independently runnable while the UI direction settles.
